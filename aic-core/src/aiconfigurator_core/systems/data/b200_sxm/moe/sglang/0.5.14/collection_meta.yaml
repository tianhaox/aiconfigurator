# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

schema_version: 1
provenance: legacy
runtime:
  framework: sglang
  version: 0.5.14
# 2026-08-09: appended 3078 MiniMax-M3 MoE shape rows (6144/3072/128x4,
# model-filtered internal collection CI run, 8x B200, zero failures; 7
# duplicate retry keys deduplicated keep-last per repo policy).
tables:
  moe_perf:
    # 2026-07-28: appended 3078 Kimi-K3 w4a16_mxfp4 rows (3584/3072, 896x16,
    # sglang_flashinfer_trtllm_moe, TP 1-32 x EP 1-128 x 3 distributions)
    # collected on 1x B200; zero failures, keys disjoint from prior rows.
    # 2026-08-01: appended 3078 Kimi-K3 w4a8_mxfp4_mxfp8 rows (same grid,
    # mxfp8 activations — the Blackwell serving default the SDK resolver
    # queries) collected on 8x B200 in the pinned stock 0.5.14 image;
    # zero failures, keys disjoint (dtype axis differs).
    status: complete
